import React from 'react';

export const SORT_COLUMNS = (<div>
  <p>Sort columns (along x axis) in order of size of cohort population. <br/>
    The arrow points in the direction of the sort (right arrow → ascending)</p>
</div>);
export const SORT_ROWS = (<div>
  <p>Sort rows (along y axis) in order of size of cohort population. <br/>
    The arrow points in the direction of the sort (down arrow ↓ ascending)</p>
</div>);;